# Core
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0
pydantic==2.10.4
pydantic-settings==2.7.0

# Playwright
playwright==1.49.1

# HTTP
httpx==0.28.1
# 0.7.4 predates chrome131 (its bundled libcurl-impersonate maxes at chrome124), so the
# configured impersonate target was unsupported and every request tripped
# `Failed to setopt 47 1, curl: (43)` at handle construction → browser fallback on every
# page. 0.15.0 ships chrome131 natively with a matching bundled binary; the async
# post(impersonate=, data=, headers=, proxies=) API is unchanged.
curl_cffi==0.15.0

# Queue (Phase 2)
redis==5.2.1
rq==2.1.0

# Image dedup
Pillow==11.0.0
imagehash==4.3.1

# S3
aioboto3==13.3.0

# Misc
python-dotenv==1.0.1
loguru==0.7.3
tenacity==9.0.0
langdetect==1.0.9