__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
*.egg-info/
dist/
build/
.pytest_cache/
.mypy_cache/
.ruff_cache/

.env
.env.local
*.env

# Prod-only compose tuning (memory limits, replicas). Gitignored so `git pull` never
# clobbers it — see docker-compose.override.yml.example and README "Prod tuning".
docker-compose.override.yml

.vscode/
.idea/
*.swp
.DS_Store

*.log
logs/

ms-playwright/
.playwright/

node_modules/
.next/

/tmp/
*.local

.claude/
.claude.json
CLAUDE.md

# Backup snapshots (timestamped, superseded by git history from here on)
*.bak
*.bak.*
*.bak2*

# Live Facebook session / runtime state (secrets, not source)
parser/fb_state.json
.giga_prev
.giga_state
.tg_offset
.watchn

# Volatile proxy pool dump (15k+ lines, not source)
proxy_new.txt
